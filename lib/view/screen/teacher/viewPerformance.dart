import 'package:flutter/material.dart';

class ViewPerformance extends StatelessWidget {
  const ViewPerformance({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("viewPerformance"),
      ),
    );
  }
}
