String getDateTimeNow() {
  // return DateFormat('hh:mm:ss yyyy-MM-dd').format(DateTime.now());
  // return DateFormat('yyyy-MM-dd hh:mm:ss').format(DateTime.now());
  return DateTime.now().toString();
}
